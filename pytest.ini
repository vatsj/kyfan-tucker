[pytest]
testpaths = tests
markers =
    slow: takes ~10-60 s (s3 symmetric search, exhaustive certificate check)
    veryslow: takes minutes (z9: S^3 degree-3 lower bound ~7 min; S^2 Q d=5 certificate ~5 min)
addopts = -m "not veryslow"
